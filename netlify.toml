[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  # Pinned so the build environment lives in the repository rather than
  # only in the Netlify dashboard. Keep in sync with .nvmrc.
  NODE_VERSION = "22"
